name: narwhals-bio
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - ipykernel
  - jupyterlab
  - pandas>=2.1
  - polars>=0.20
  - pyarrow>=15
  - fastparquet
  - matplotlib
  - numpy
  - packaging
  - pip:
      # Use pip to grab the freshest Narwhals
      - narwhals>=0.12
      # OPTIONAL: enable Modin backend (heavy; comment out if not needed)
      # - "modin[ray]>=0.29"
      # - ray>=2.9
